[pytest]
# Run engine tests from the backend/ root so `import faraid_engine` resolves without
# installing the package. The engine has zero Django dependency (CLAUDE.md), so these
# tests need no Django settings.
testpaths = faraid_engine/tests
python_files = test_*.py
addopts = -q
