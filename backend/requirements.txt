# Backend dependencies. The faraid_engine package itself has ZERO third-party deps.
Django>=4.2,<5.1
djangorestframework>=3.14
django-cors-headers>=4.0
dj-database-url>=2.0
psycopg2-binary>=2.9   # PostgreSQL driver (prod); SQLite is used if DATABASE_URL is unset
pytest>=8.0
pytest-django>=4.7
