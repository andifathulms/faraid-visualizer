/node_modules
/.next
/out
/build
next-env.d.ts
.env*.local
*.tsbuildinfo

# Generated by scripts/prepare-python.mjs on every build — the Python sources are
# bundled from backend/, and the Pyodide runtime is vendored from node_modules.
# Never commit these: they would go stale against the engine they mirror.
/public/py
/public/pyodide
